# Secure Agent Tool 安全重构需求文档

- 文档状态：草案
- 文档版本：1.2
- 基线项目：agent-tool v0.9.1
- 目标读者：产品负责人、架构师、安全工程师、开发与测试工程师

---

## 1. 文档目的

本文档用于定义新一代高安全级 AI Agent 工具平台（下称“Secure Agent Tool”）的产品范围、功能需求、安全要求、目标架构、迁移策略与验收标准。

项目以现有 `agent-tool` 的功能和工程经验为基础，但不直接延续其单进程、共享权限的安全架构。新系统应选择性复用编码处理、代码分析等纯计算模块，并重新设计权限模型、执行隔离、用户审批和审计机制。

---

## 2. 项目背景

现有 `agent-tool` 是一个通过 MCP（Model Context Protocol）为 Claude Code、Codex、Cursor、Windsurf 等 AI 编程客户端提供工具能力的 Go 项目，主要覆盖：

- 编码感知的文件读取与编辑；
- 文件搜索、比较、压缩、备份与编码转换；
- Shell、进程和系统管理；
- HTTP、DNS、SSH、SFTP 和数据库访问；
- DAP 动态调试；
- PE、ELF、Mach-O 静态分析；
- 进程内存扫描与修改；
- 基于 tree-sitter 和 SQLite 的代码图索引。

现有项目功能完整、部署简单，适合本地单用户开发场景，但其核心信任模型是 MCP 客户端、AI 和当前用户环境基本可信。文件、网络、Shell、调试、内存修改等能力集中在同一进程内，无法满足恶意仓库、提示词注入、多用户或生产环境下的高安全要求。

---

## 3. 建设目标

### 3.1 核心目标

1. 在保留现有项目文件编码、智能编辑和代码分析优势的基础上，建立默认拒绝、最小权限的工具执行平台。
2. 将 MCP 控制面、策略判断和高风险执行能力分离，避免单个漏洞获得全部宿主权限。
3. 强制限制文件访问范围，防止访问工作区以外的密钥、配置和其他项目。
4. 隔离本地文件能力和外部网络能力，降低数据外传风险。
5. 对写文件、执行命令、访问网络和连接远程系统等操作提供分级授权和带外审批。
6. 建立完整、不可静默关闭的安全审计链路。
7. 支持渐进式交付，从安全文件工具扩展到沙箱执行和受控网络访问。

### 3.2 成功标准

- 默认配置下，AI 只能读取明确授权工作区内的普通文件。
- 任意工具无法通过 `..`、符号链接、硬链接、重解析点或竞态条件逃逸工作区。
- 文件读取 Worker 无外网能力，网络 Worker 无工作区读取能力。
- AI 无法通过 MCP 工具调用降低管理员安全策略。
- 高风险操作未经可信 UI 或宿主侧批准不得执行。
- 每次敏感操作均能关联到用户、会话、请求、策略决策、审批和执行结果。
- Worker 被攻破后，其影响范围仍被操作系统沙箱和短期能力令牌限制。

---

## 4. 非目标

首期不以以下内容为目标：

- 提供完全兼容现有全部 50 余种工具的单一可执行文件；
- 允许 AI 无审批执行任意宿主机命令；
- 通过命令字符串黑名单实现 Shell 安全；
- 将正则 DLP 作为防止数据外传的主要安全边界；
- 在首期提供进程内存修改、GUI 自动化或生产服务器 SSH 控制；
- 试图在所有操作系统上提供完全相同等级的原生沙箱；
- 为不受信任用户提供共享宿主机上的无隔离多租户执行。

---

## 5. 设计原则

1. **默认拒绝**：未明确授权的能力、路径、域名和操作一律拒绝。
2. **最小权限**：每个 Worker 仅获得完成当前任务所需的权限。
3. **能力隔离**：文件、网络、命令、远程连接和调试能力分离部署。
4. **纵深防御**：应用校验、能力令牌、操作系统沙箱和审计同时生效。
5. **策略不可降级**：AI 和普通会话只能收紧策略，不能放宽管理员策略。
6. **显式授权**：高风险操作必须展示实际影响后由可信主体批准。
7. **无隐式环境继承**：Worker 不继承宿主敏感环境变量、凭据和用户配置。
8. **结果可验证**：写操作支持 dry-run、文件哈希和变更范围限制。
9. **失败安全**：策略服务、审批服务或审计服务异常时默认拒绝敏感操作。
10. **安全优先于兼容**：当兼容性与安全边界冲突时，优先保证安全。

---

## 6. 现有项目优势与保留要求

### 6.1 编码感知文件处理

必须保留并增强以下能力：

- BOM、`.editorconfig`、UTF-16 特征、UTF-8 合法性、chardet 和 fallback 的分层编码识别；
- UTF-8、UTF-8 BOM、UTF-16 LE/BE、EUC-KR、Shift-JIS、ISO-8859-1 等编码；
- 写回时保留原始编码、BOM、换行符和文件权限；
- 编码置信度提示；
- 原子写入，避免部分写入破坏文件；
- 文件大小限制，避免内存耗尽。

重构要求：编码算法可以复用，但文件打开、路径解析和写入必须通过新的安全文件系统接口完成，不得由算法模块直接使用任意绝对路径。

### 6.2 智能缩进与精确编辑

必须保留：

- `.editorconfig` 缩进规则读取；
- Tab 与空格自动适配；
- 精确字符串替换；
- 单次替换和全部替换；
- dry-run 差异预览；
- SHA-256 `expected_hash` 乐观并发控制；
- 多位置编辑的原子性。

增强要求：

- 写操作默认要求 `expected_hash`；
- 可限制最大变更文件数、字节数和行数；
- 策略可禁止创建、删除、移动或修改可执行文件；
- 提交前必须重新校验目标文件身份，防止 TOCTOU。

### 6.3 MCP 参数容错

可保留 `SafeAddTool` 的以下思想：

- 自动生成 JSON Schema；
- 默认值应用和严格校验；
- 可控的整数与字符串数组参数纠错；
- panic recovery；
- 将可重试错误转换为清晰的工具错误。

增强要求：

- 容错不得绕过安全字段的严格类型要求；
- 权限令牌、路径、域名、命令和审批字段不得进行模糊纠正；
- 错误输出不得泄露宿主路径、环境变量、密钥或内部调用栈。

### 6.4 静态分析与代码图

可选择性复用：

- PE、ELF、Mach-O 纯解析；
- x86/x64/ARM/ARM64 反汇编；
- 字符串、熵、调用图、xref、RTTI 和虚表分析；
- tree-sitter WASM 解析；
- 符号、调用者、被调用者、继承和 import 查询。

安全要求：

- 解析器必须运行在只读 Analysis Worker 中；
- 默认无网络；
- 对 CPU、内存、输出和运行时间设置限制；
- 不信任输入文件格式，必须进行模糊测试和异常恢复；
- 索引数据库只能写入专用状态目录，不得任意写入源码目录。

### 6.5 跨平台与单文件部署体验

保留 Go 作为主要实现语言，以获得：

- 较低部署成本；
- 明确的跨平台构建；
- 易于静态发布；
- 良好的并发和系统调用支持。

“单文件体验”可以通过 Launcher 管理多个 Worker 实现，但不得为追求单进程而破坏权限隔离。

---

## 7. 威胁模型

### 7.1 需要防御的威胁

- 恶意网页中的提示词注入；
- 恶意仓库中的 README、注释、测试或配置指令；
- AI 误判或生成危险参数；
- 恶意 MCP 客户端直接调用工具；
- 路径穿越、符号链接逃逸和 TOCTOU；
- DNS rebinding、重定向 SSRF、代理绕过和云元数据访问；
- 读取本地密钥后通过 HTTP、DNS、SSH、数据库或 Shell 外传；
- 恶意压缩包、恶意二进制和解析器漏洞；
- 命令执行造成持久化、提权、资源耗尽或横向移动；
- Worker 或第三方依赖被攻破；
- 审批重放、能力令牌伪造和会话混淆；
- 日志注入及日志中泄露秘密；
- 多个 Agent 会话之间的数据串扰。

### 7.2 信任边界

默认不信任：

- AI 输出；
- MCP 请求参数；
- 仓库内所有文件；
- 外部网页和网络响应；
- 被分析的二进制和压缩包；
- Worker 返回的非结构化文本；
- 客户端声明的“用户已批准”。

可以信任：

- 管理员预配置策略；
- 经过认证的本地审批 UI；
- Gateway 与 Policy Engine 的代码和签名密钥；
- 操作系统强制实施的沙箱边界；
- 经校验的短期能力令牌。

### 7.3 暂不覆盖的威胁

- 已完全控制宿主机内核或管理员账户的攻击者；
- 硬件、固件和 hypervisor 级攻击；
- 用户主动将生产密钥复制进获准上传的数据；
- 由底层操作系统安全缺陷导致的沙箱逃逸。

---

## 8. 目标用户与典型场景

### 8.1 本地开发者

在受控工作区内允许 AI 阅读和编辑代码，但禁止访问个人密钥、浏览器数据和其他项目。

### 8.2 企业开发环境

企业管理员统一设置文件、网络、命令和审批策略，并获取完整审计记录。

### 8.3 CI/自动化 Agent

以无交互模式执行预批准的只读分析、测试或格式化任务，能力由流水线身份和固定策略限定。

### 8.4 安全分析人员

在隔离 Analysis Worker 中分析不可信二进制，不允许解析器访问宿主文件和网络。

---

## 9. 目标架构

```text
本地模式：
MCP Client <-- MCP stdio --> Local Gateway

远程模式：
MCP Client <-- MCP stdio --> Local Stdio Bridge <-- HTTP（受控内网）/ HTTPS（公网） --> Remote Gateway

Local/Remote Gateway  ----->  Audit Service
          |
          v
Policy Engine <---- Approval Service / Trusted UI
          |
          | short-lived signed capability
          +----------+----------+-----------+-------------+
          v          v          v           v             v
      Read Worker  Write Worker Exec Worker Network Worker Analysis Worker
      只读工作区   受控写入     沙箱执行    无本地文件    只读单文件
      无网络       无网络       默认断网    域名受限      无网络
```

远程部署在受控内网中可使用 HTTP，在公网、跨不可信网络或无法确认网络边界时必须使用 HTTPS。系统必须提供本地 Stdio Bridge，使仅支持 MCP stdio 的客户端仍可接入。Bridge 负责从 `stdin` 读取 MCP 消息、通过配置的 HTTP(S) 端点转发，并将远端响应原样写入 `stdout`；不得把 `stdio` 直接暴露为网络监听端口。

### 9.1 MCP Gateway

职责：

- MCP 握手、工具描述和请求接收；
- 身份、会话和请求 ID 管理；
- 参数语法校验；
- 调用 Policy Engine；
- 请求审批；
- 签发或转发能力令牌；
- 汇总结构化结果；
- 记录审计事件。

限制：

- 不直接打开工作区文件；
- 不直接访问外网；
- 不执行 Shell；
- 不持有 SSH、数据库和云凭据。

### 9.2 Policy Engine

职责：

- 合并管理员、项目、用户和会话策略；
- 计算工具风险等级；
- 校验路径、域名、资源上限和能力组合；
- 返回允许、拒绝或需要审批；
- 生成机器可读的决策理由。

策略优先级：

```text
管理员硬限制 > 部署策略 > 项目策略 > 用户策略 > 会话策略 > 请求参数
```

低层级策略只能收紧，不能放宽高层级策略。

### 9.3 Approval Service

职责：

- 在可信 UI 中展示规范化后的真实操作；
- 验证用户身份；
- 签发短期、单次使用的审批令牌；
- 防止 AI 伪造用户批准。

### 9.4 Worker

所有 Worker 必须：

- 独立进程或独立容器；
- 使用独立低权限身份；
- 验证能力令牌；
- 不接受令牌授权范围外的参数；
- 限制 CPU、内存、文件数、进程数、输出和超时；
- 不继承宿主敏感环境变量；
- 将结果以结构化协议返回；
- 在异常时终止并清理临时状态。

### 9.5 传输层与 Stdio Bridge

传输层分为客户端接入和内部调用两类，二者不得混用：

- **本地接入**：Gateway 或 Stdio Bridge 必须能够持续读取 `stdin` 并向 `stdout` 写入符合 MCP 版本要求的消息；
- **内网远程接入**：Stdio Bridge 与 Remote Gateway 可使用 HTTP，但仅限管理员明确配置的受控内网；
- **公网远程接入**：必须使用 HTTPS，建议采用支持流式响应的 HTTP/2；
- **内部调用**：Gateway、Policy、Audit 与 Worker 之间使用经过认证的本地 IPC 或 mTLS，不因外部采用 HTTP 而放宽内部信任边界。

HTTP 内网模式要求：

- 默认关闭，只能由部署或管理员策略显式开启，项目文件、用户输入和 MCP 请求不得开启该模式；
- Gateway 只能绑定指定内网接口或私有地址，不得同时监听公网接口；反向代理、NAT 或端口映射不得将该端点发布到公网；
- “内网”必须由配置的 CIDR、网络接口和可信代理列表判定，不能仅根据域名后缀、Host Header 或客户端自报地址判定；
- 即使使用 HTTP，仍必须执行客户端认证、请求签名或短期令牌校验、时间戳/nonce 防重放、限流和完整审计；
- 健康检查与管理接口应独立鉴权，不得通过 HTTP 暴露调试、策略修改或凭据读取能力；
- 管理界面必须明确提示 HTTP 不提供传输机密性，无法防御同网段嗅探或中间人攻击；承载敏感源码、凭据或高风险操作时应改用 HTTPS，或在 WireGuard、IPsec 等加密隧道内使用 HTTP。

Stdio Bridge 要求：

- 只承担协议适配、认证和传输，不执行工具、不直接读取工作区，也不缓存完整请求或响应；
- `stdout` 只能输出 MCP 协议数据，诊断日志必须写入 `stderr`，且同样执行脱敏；
- 保持 JSON-RPC/MCP 消息边界、请求 ID、顺序语义和取消信号，不得把半条消息发送给远端；
- 对单条消息、并发请求、缓冲区和等待时间设置上限，并实现背压，防止慢客户端或慢网络造成无界内存增长；
- HTTP 请求必须携带会话与请求关联信息，但不得仅凭客户端提供的 Request ID 完成鉴权；
- 公网模式必须使用 TLS 1.2 及以上并校验证书和主机名；企业部署应支持 mTLS 或短期访问令牌；内网 HTTP 模式不得静默回退或自动跳过 TLS 校验；
- 认证令牌从受控凭据源获得，不允许通过 MCP 参数传入、输出到 `stdout` 或记录到日志；
- 网络断开时，对未完成请求返回明确的传输错误；除非操作具备幂等键且服务端确认未执行，否则不得自动重放写入或高风险请求；
- Bridge 退出时应取消关联请求、清理内存中的认证材料，并使用可预测的非零退出码报告启动或传输故障。

HTTP 接口要求：

- 明确协议版本和内容类型，拒绝不支持的媒体类型、超大请求、非法压缩和歧义编码；
- 将客户端身份、会话身份和每次请求绑定，并在鉴权后由服务端生成或确认 Request ID；
- 支持流式返回长任务进度、最终结果和结构化错误；心跳、取消和超时语义必须有版本化定义；
- CORS 默认关闭，不使用 Cookie 作为非浏览器 Agent 的默认认证方式；
- HTTP 传输仅改变消息通道，不授予远端 Gateway 访问客户端本地文件、环境变量或 `stdio` 之外资源的能力；
- 如需将本地文件内容发送到远端，必须通过已授权工具显式读取，并经过上传审批和数据外传策略，不得由 Bridge 隐式读取。

---

## 10. 能力令牌需求

能力令牌至少包含：

- Token ID；
- Request ID；
- Session ID；
- 调用者身份；
- Worker 类型；
- 允许的操作；
- 路径、域名或命令约束；
- 资源限制；
- 策略版本；
- 签发和过期时间；
- 单次使用标记；
- 随机数；
- 签名或 MAC。

要求：

- 默认有效期不超过 60 秒；
- 高风险令牌必须单次使用；
- 不允许跨 Worker 使用；
- 不允许修改令牌后继续执行；
- Gateway 和 Worker 都要记录 Token ID；
- 服务重启后仍应能阻止有效期内的高风险令牌重放，或使用足够短的有效期与集中式 nonce 存储。

示例：

```json
{
  "token_id": "cap-01J...",
  "request_id": "req-01J...",
  "session_id": "session-01J...",
  "worker": "write",
  "operation": "replace",
  "root_id": "workspace-project-a",
  "paths": ["src/main.go"],
  "expected_sha256": "...",
  "max_changed_lines": 30,
  "expires_at": "2026-03-10T12:00:30Z",
  "single_use": true
}
```

---

## 11. 工具风险分级

### 11.1 L0：纯计算

示例：

- 文本编码转换（仅处理请求内数据）；
- Diff 计算；
- Hash 计算（仅处理已授权输入）；
- 结构化数据查询；
- 纯格式转换。

默认策略：可自动批准，但仍需限制输入和输出大小。

### 11.2 L1：工作区只读

示例：

- read、multiread；
- grep、glob、listdir；
- file_info、checksum、sloc；
- CodeGraph 查询；
- 静态分析。

默认策略：允许访问授权工作区中的非敏感文件，禁止工作区外访问。

### 11.3 L2：工作区写入

示例：

- edit、multiedit；
- write、patch；
- copy、rename、mkdir；
- 编码转换。

默认策略：需要变更预览；敏感目录、批量修改和创建可执行文件需要审批。

### 11.4 L3：外部网络或受限命令

示例：

- WebFetch；
- Download；
- 沙箱中的测试、构建和格式化命令。

默认策略：目标和命令必须命中白名单；网络与工作区读取能力不得隐式组合。

### 11.5 L4：高风险系统能力

示例：

- 任意 Shell；
- ProcExec/ProcKill；
- SSH/SFTP；
- MySQL/Redis 写入；
- Debug/Memtool；
- GUI 自动化；
- 文件删除；
- 任意 HTTP 方法与请求体上传。

默认策略：不安装或不注册；启用后每次显式审批，并在独立 Worker 中执行。

---

## 12. 功能需求

### FR-001 MCP 服务

- 本地模式支持 MCP stdio 传输；
- 远程 Agent 在受控内网可使用 HTTP，在公网或不可信网络必须使用 HTTPS，并提供可执行的本地 Stdio Bridge，以兼容仅能通过 `stdin`/`stdout` 通信的 MCP 客户端；
- Stdio Bridge 必须正确转发请求、通知、流式响应、取消和结构化错误，日志只能写入 `stderr`；
- HTTP 与 stdio 接入使用同一套工具 Schema、策略判断和审计语义，切换传输方式不得提升权限；
- 后续可选支持经过认证和加密的本地 socket；
- 能按当前身份和策略动态展示可用工具；
- 禁止向无权限客户端暴露高风险工具的可调用入口；
- 所有请求必须生成唯一 Request ID，并可在 stdio、HTTP、Gateway 和 Worker 全链路关联；
- 所有远程请求必须认证并限制请求体、响应体、并发数、速率和超时；公网请求必须加密，内网 HTTP 请求必须满足 9.5 节的额外限制。

### FR-002 工作区管理

- 工作区必须由可信宿主或管理员创建；
- AI 不得自行将任意目录设为工作区；
- 工作区使用不可猜测的内部 ID，对 Worker 隐藏不必要的宿主绝对路径；
- 支持只读和读写工作区；
- 支持项目级排除路径和敏感路径规则；
- 工作区授权必须有生命周期，可撤销。

默认拒绝访问：

- `.ssh`、`.aws`、`.gnupg`；
- 浏览器和密码管理器目录；
- 系统凭据目录；
- `.env`、私钥和 token 文件，除非显式授权；
- 版本控制凭据和远程认证配置。

### FR-003 安全路径解析

- 所有路径必须相对于授权根目录表达；
- 拒绝绝对路径、NUL、非法编码和平台保留名；
- 防止 `..` 逃逸；
- 默认拒绝符号链接、junction 和 reparse point；
- 必须在打开文件时强制“位于根目录下”，不能仅依赖预检查；
- Linux 优先使用目录 FD 和 `openat2(RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS)`；
- Windows 使用句柄级最终路径校验和 reparse point 策略；
- 对目标文件身份进行打开前后校验，降低 TOCTOU 风险。

### FR-004 安全读取

- 支持编码检测、行范围、大小限制和图片读取；
- 默认单文件最大读取量可配置，且受管理员上限约束；
- 输出超限时截断并明确标注；
- 读取二进制时不得误作为文本返回；
- 敏感文件匹配策略时拒绝或申请审批；
- 不在错误信息中返回工作区外真实路径。

### FR-005 安全编辑与写入

- 默认先执行 dry-run；
- 编辑请求应包含原始文件 SHA-256；
- 写入前重新验证哈希；
- 支持编码、BOM、换行和权限保留；
- 默认使用同目录安全临时文件和原子替换；
- 限制最大修改文件数、行数和字节数；
- 禁止修改超出能力令牌范围的文件；
- 修改可执行文件、CI 发布脚本、认证配置等高敏感目标时升级审批等级；
- 多文件事务至少保证“全部预检查通过后才开始写入”，并提供失败恢复或备份策略。

### FR-006 搜索与索引

- grep/glob/listdir 只能遍历授权目录；
- 必须遵守排除规则和嵌套 `.gitignore`；
- 限制遍历深度、文件数、匹配数和总读取字节；
- CodeGraph 索引存储在系统状态目录；
- 索引必须按工作区和用户隔离；
- 删除工作区授权时支持清理对应索引。

### FR-007 沙箱命令执行

- 首期不提供宿主机任意 Shell；
- 命令在独立容器、轻量 VM 或等价系统沙箱内执行；
- 默认无网络；
- 根文件系统只读；
- 工作区默认只读挂载，写入需独立能力；
- 使用固定工具链镜像或管理员白名单可执行文件；
- 不继承宿主 PATH、HOME、SSH Agent 和敏感环境变量；
- 设置 CPU、内存、PID、磁盘、打开文件数、输出和超时限制；
- 默认每次请求创建临时环境；
- 如支持持久会话，必须按用户和工作区隔离，并设置短空闲超时。

### FR-008 网络访问

- 网络能力由独立 Network Worker 提供；
- Network Worker 不挂载工作区；
- 默认仅支持 GET/HEAD；
- POST、PUT、PATCH、DELETE 和上传必须升级审批；
- 每次 DNS 解析、连接和重定向都必须重新执行 SSRF 策略；
- 云元数据、回环、链路本地和未授权私网地址始终拒绝；
- 管理员可配置域名、端口和协议白名单；
- 禁止 AI 任意设置代理，除非代理由管理员预配置；
- 限制请求头、响应大小、重定向次数和超时；
- 外部内容必须附带结构化“不可信内容”标识，而不只添加文本警告。

### FR-009 数据外传控制

- 文件 Worker 不具备网络能力；
- Network Worker 不具备工作区文件能力；
- 上传内容必须由 Gateway 显式传递，并关联读取授权和上传审批；
- 支持内容分类、大小限制和敏感模式扫描；
- DLP 仅作为辅助检测，不作为唯一安全边界；
- 禁止通过 DNS、错误日志、Header 和 URL 参数隐式传输未经授权的数据；
- 审计记录上传数据来源、摘要、大小和目标，不默认记录完整秘密内容。

### FR-010 远程连接与数据库

- SSH、SFTP、MySQL、Redis 作为可选高风险插件；
- 插件必须与核心 Gateway 隔离；
- 凭据由宿主安全凭据服务注入，AI 不得读取明文；
- 目标主机、用户、数据库和允许操作由策略限制；
- 默认只读数据库查询；
- 危险命令和 DDL/DML 需要额外审批；
- 禁止将凭据写入日志或工具输出。

### FR-011 调试、内存和 GUI 工具

- Debug、Memtool、Wintool 不纳入默认发行能力；
- 启用时使用专用高风险 Worker；
- 仅允许附加到由同一安全会话创建或明确批准的进程；
- 禁止默认启用系统调试特权；
- 内存写入、进程注入和 GUI 消息发送必须单独审批；
- 企业部署可在策略层永久禁用这些能力。

### FR-012 配置管理

配置分为：

1. 管理员不可降级策略；
2. 部署配置；
3. 项目策略；
4. 用户偏好；
5. 会话临时限制。

要求：

- AI 只能修改非安全偏好，或进一步收紧会话策略；
- `allow_private_network`、`allow_symlink`、`enable_shell` 等不得由普通 MCP 工具放宽；
- 配置变更必须记录操作者和差异；
- 管理员策略支持签名与版本控制。

### FR-013 安装与集成

- 支持 Claude Code、Codex、Cursor、Windsurf 和标准 MCP 客户端；
- 远程安装应自动生成最小化的 Stdio Bridge 启动配置，但必须在写入客户端配置前展示远端地址、认证来源和配置差异；
- Bridge 的远端地址必须来自管理员配置或用户显式确认，不得由仓库内容或 MCP 请求动态覆盖；
- 默认安装模式不得自动批准全部工具；
- 默认只注册 L0/L1 工具；
- 安全模式必须是默认且不可使用模糊命名误导用户；
- 安装程序修改配置前必须展示变更并创建备份；
- 卸载应仅删除自身配置并保留用户其他配置；
- 高风险插件必须单独安装和启用。

### FR-014 审计

每次请求至少记录：

- 时间；
- 用户、客户端和会话身份；
- 接入传输类型、Bridge 实例 ID 和认证主体；远端 IP 仅按隐私策略记录；
- Request ID、Token ID；
- 工具和规范化参数摘要；
- 工作区、目标路径或网络目标；
- 策略版本与决策；
- 是否审批及审批者；
- Worker 身份；
- 开始、结束、耗时和结果状态；
- 输入输出大小；
- 文件变更前后哈希。

要求：

- 日志采用结构化格式；
- 对换行和控制字符编码，防止日志注入；
- 密钥、Authorization、Cookie、密码和私钥不得写入日志；
- 企业版支持写入追加式或远程不可篡改存储；
- 审计失败时，L2 及以上操作默认拒绝。

---

## 13. 非功能需求

### NFR-001 安全

- 采用最小权限和默认拒绝；
- 对安全核心代码进行威胁建模和独立代码审计；
- 关键依赖锁定版本并生成 SBOM；
- 发布物提供签名和校验值；
- 支持依赖漏洞扫描、静态分析、模糊测试和秘密扫描；
- 关键路径达到高覆盖率测试要求。

### NFR-002 性能

建议目标：

- Gateway 空闲内存低于 100 MB；
- 普通 MCP 请求策略判断 P95 低于 20 ms，不含审批和 Worker 执行；
- 1 MB 文本读取 P95 低于 200 ms；
- 小文件编辑 P95 低于 300 ms；
- Worker 冷启动目标低于 1 秒，容器/VM 平台可按实现调整；
- 大文件和二进制分析必须流式或分段处理，避免无界内存增长。

### NFR-003 可用性

- 工具错误必须包含明确、可操作且不泄密的信息；
- dry-run 输出应易于审阅；
- 审批界面展示最终规范化参数，而不是原始含歧义输入；
- 安全拒绝应指出违反的策略 ID；
- 支持诊断模式，但诊断信息同样需要脱敏。

### NFR-004 可靠性

- Worker 崩溃不得导致 Gateway 崩溃；
- 请求超时后必须终止相关 Worker 任务；
- 临时文件和容器应自动清理；
- 原子写入失败不得破坏原文件；
- 审批、能力和审计事件应具备幂等标识。

### NFR-005 可维护性

- 工具注册应采用声明式 Registry，而不是在入口文件逐个手工注册；
- 每个工具声明风险等级、Worker 类型、资源需求和审批规则；
- 安全 I/O 通过统一接口实现，业务模块不得绕过；
- 平台相关实现与业务逻辑分离；
- 文档、策略 Schema 和工具 Schema 纳入版本管理。

### NFR-006 兼容性

- 首期支持 Linux，macOS 和 Windows 根据沙箱能力分阶段支持；
- 对无法达到同等隔离等级的平台，必须明确降级并默认禁用高风险能力；
- MCP 协议兼容性应有自动化测试；
- stdio、内网 HTTP 与公网 HTTPS 接入对同一请求应产生等价的策略决策、结果结构和审计关联。

---

## 14. 安全审批体验

审批界面必须展示：

- 工具风险等级；
- 完整规范化操作；
- 真实工作区和相对路径；
- 文件修改 diff；
- 命令、参数、工作目录和环境变量名称；
- 网络协议、域名、解析 IP 和端口；
- 是否上传数据及数据来源、大小和摘要；
- 操作是否持久化；
- 权限有效期。

审批选项：

- 拒绝；
- 仅允许本次；
- 本会话允许相同的严格规则；
- 创建长期规则，仅管理员或授权用户可用。

禁止使用笼统的“允许 Agent 执行所有后续操作”。

---

## 15. 平台沙箱要求

### 15.1 Linux

优先采用：

- 独立 UID/GID；
- user、mount、PID 和 network namespace；
- 只读 rootfs 和 bind mount；
- seccomp；
- Landlock；
- AppArmor 或 SELinux；
- cgroups v2；
- `openat2` 安全路径解析。

### 15.2 macOS

优先采用：

- 独立低权限用户；
- 受控容器或轻量 VM；
- 明确授权目录；
- 不支持可靠隔离时禁用高风险 Worker。

### 15.3 Windows

优先采用：

- Restricted Token；
- AppContainer；
- Low Integrity Level；
- Job Object；
- ACL 和句柄继承限制；
- reparse point 防护；
- 禁止默认启用 `SeDebugPrivilege`。

---

## 16. 现有模块处置建议

| 现有模块 | 处置 | 说明 |
|---|---|---|
| 编码检测与转换 | 重构复用 | 保留算法，替换底层文件访问 |
| 智能缩进与替换 | 重构复用 | 增加哈希、范围和审批约束 |
| Diff/Checksum/SLOC | 直接或轻量重构 | 放入 L0/L1 Worker |
| JSON/YAML/TOML Query | 轻量重构 | 限制输入大小和深度 |
| SafeAddTool | 重构复用 | 安全字段必须严格解析 |
| Analyze | 隔离复用 | 进入只读、无网络 Analysis Worker |
| CodeGraph | 隔离复用 | 索引迁移至专用状态目录 |
| Compress/Decompress | 重构 | 强化 Zip Bomb、链接和权限策略 |
| WebFetch/HTTP | 重新实现边界 | 独立 Network Worker，逐跳 SSRF 校验 |
| Bash/ProcExec | 不直接复用 | 改为容器或 VM 沙箱任务 |
| SSH/SFTP | 插件化重构 | 凭据托管，目标和命令白名单 |
| MySQL/Redis | 插件化重构 | 默认只读，高风险语句审批 |
| Debug | 高风险插件 | 仅附加受控进程 |
| Memtool/Wintool | 默认移除 | 特殊场景单独启用 |
| set_config | 重新设计 | AI 不得降低安全策略 |
| install | 重构 | 默认无自动批准，高危插件独立安装 |
| 父进程监控 | 保留增强 | 同时管理 Worker 生命周期 |

---

## 17. 数据模型

### 17.1 核心实体

- Principal：用户、服务账号或客户端身份；
- Session：一次 MCP 会话；
- Workspace：受控文件根目录；
- Policy：管理员、项目或用户策略；
- Request：一次工具调用；
- Decision：策略决策；
- Approval：可信用户审批；
- Capability：短期执行权限；
- WorkerJob：实际执行任务；
- AuditEvent：不可抵赖审计事件。

### 17.2 状态隔离

- 所有状态必须绑定租户、用户和工作区；
- 不同会话的 Shell、索引、缓存和临时文件不得共享；
- 默认不持久保存工具原始输出；
- 缓存不得跨安全域复用敏感数据。

---

## 18. 测试需求

### 18.1 单元测试

必须覆盖：

- 编码检测和往返写入；
- 路径规范化和平台保留路径；
- 符号链接和 reparse point；
- 能力令牌签名、过期、绑定和重放；
- 策略合并及不可降级；
- 日志脱敏；
- 编辑哈希冲突和原子写入；
- SSRF 地址分类和逐跳重定向。

### 18.2 集成测试

至少包括：

- Gateway → Policy → Approval → Worker 完整链路；
- MCP Client → Stdio Bridge → HTTP(S) → Remote Gateway 完整链路；
- stdio 消息分帧、并发、背压、取消、异常退出和 `stdout` 无日志污染；
- 内网 HTTP 的来源网段、监听接口、请求签名、防重放及公网暴露检测；
- HTTP(S) 鉴权失败、HTTPS 证书错误、限流、断线，以及非幂等请求不被自动重放；
- Worker 崩溃和超时；
- 审计服务不可用；
- 工作区撤销；
- 并发编辑同一文件；
- 容器网络和文件系统隔离；
- 多用户、多工作区和多会话隔离。

### 18.3 安全测试

至少覆盖：

- `../`、绝对路径、Unicode 混淆路径；
- 符号链接交换竞态；
- 压缩包路径穿越和压缩炸弹；
- DNS rebinding、CNAME、IPv4/IPv6 混淆和重定向 SSRF；
- 云元数据访问；
- 请求 Header、URL、DNS 和日志数据外传；
- 命令注入和 Shell 逃逸；
- 能力令牌篡改、重放和跨 Worker 使用；
- 伪造 Bridge 身份、HTTP 请求走私、压缩炸弹、超大帧和跨会话响应串扰；
- 网络断开后写入请求被错误重放，以及取消请求后服务端仍继续执行；
- 审批参数与执行参数不一致；
- 恶意 PE/ELF/Mach-O、图片、YAML 和 tree-sitter 输入；
- 资源耗尽。

### 18.4 模糊测试

对以下模块持续运行 fuzz：

- 编码检测；
- 路径解析；
- Patch/Diff；
- 压缩包解析；
- PE/ELF/Mach-O；
- MCP 参数解析；
- 能力令牌解析；
- HTML/Markdown 转换。

### 18.5 现有测试整改

- 移除依赖开发者本机绝对路径的测试；
- 使用仓库内最小测试 fixture 或测试时动态生成样本；
- 所有测试必须可在 Linux、macOS、Windows CI 中重复运行；
- 平台专属测试应使用 build tag 并明确跳过原因。

---

## 19. 发布与供应链要求

- 使用锁定依赖和可重复构建；
- 生成 SPDX 或 CycloneDX SBOM；
- 发布二进制及容器镜像必须签名；
- 提供 SHA-256 校验值；
- CI 使用最小权限凭据；
- 自动执行依赖漏洞、许可证和恶意包扫描；
- 关键 Worker 镜像使用固定 digest；
- 制定安全漏洞报告和响应流程；
- 高危漏洞支持快速禁用相关工具或 Worker。

---

## 20. 分阶段实施计划

### Phase 0：安全设计与基础设施

交付：

- 威胁模型；
- Policy Schema；
- 工具风险分级；
- Gateway/Worker 通信协议；
- 能力令牌规范；
- 审计事件规范；
- Linux 沙箱 PoC。

退出条件：安全评审通过，证明 Worker 无法访问未授权目录和网络。

### Phase 1：安全文件工具 MVP

交付：

- MCP Gateway；
- 本地 Stdio Bridge、内网 HTTP 与公网 HTTPS 接入；
- Policy Engine；
- Read Worker；
- Write Worker；
- read、multiread、grep、glob、listdir、file_info、checksum、diff、edit、write；
- 工作区管理；
- dry-run、hash 和审计；
- 基础审批 UI。

退出条件：完成路径逃逸、并发写入、审计和多会话隔离验收。

### Phase 2：代码分析

交付：

- Analysis Worker；
- SLOC；
- JSON/YAML/TOML 查询；
- CodeGraph；
- PE/ELF/Mach-O 静态分析；
- 解析器资源限制和 fuzz 测试。

### Phase 3：沙箱执行

交付：

- Exec Worker；
- 容器/VM 执行；
- 固定工具链；
- 默认断网；
- 构建、测试、格式化任务；
- 资源限制和任务清理。

### Phase 4：受控网络

交付：

- Network Worker；
- WebFetch 和 Download；
- 域名策略；
- 逐跳 SSRF 防护；
- 上传审批和来源追踪。

### Phase 5：企业高风险插件

按需交付：

- SSH/SFTP；
- 数据库；
- Debug；
- Memtool；
- GUI 自动化。

每个插件必须独立完成威胁模型、安全评审和启用审批。

---

## 21. MVP 验收标准

### 安全验收

- [ ] 默认仅启用 L0/L1 工具；
- [ ] 默认安装不配置全工具自动批准；
- [ ] AI 无法新增任意工作区；
- [ ] 工作区外文件读取测试全部失败；
- [ ] 符号链接和竞态逃逸测试全部失败；
- [ ] Read Worker 无网络；
- [ ] Write Worker 无网络；
- [ ] Gateway 无直接文件和命令执行权限；
- [ ] AI 无法降低管理员策略；
- [ ] L2 操作具备 dry-run、哈希校验和审计；
- [ ] 审批令牌无法重放、跨会话或跨 Worker 使用；
- [ ] 审计失败时写操作被拒绝；
- [ ] 日志中不出现测试密钥和 Authorization 内容。

### 功能验收

- [ ] UTF-8、UTF-16、EUC-KR 和 Shift-JIS 文件可正确读取和往返编辑；
- [ ] BOM、换行和权限得到保留；
- [ ] Tab/空格适配符合 `.editorconfig`；
- [ ] 多文件读取和编辑具备明确上限；
- [ ] MCP 客户端能收到结构化错误和风险信息；
- [ ] 仅支持 stdio 的客户端可通过 Bridge 使用远程 HTTP(S) Agent；
- [ ] 内网 HTTP 默认关闭，启用后仅监听配置的私有接口且无法由 MCP 请求降级开启；
- [ ] 公网入口拒绝明文 HTTP，HTTPS 证书校验失败时不得降级连接；
- [ ] Bridge 的 `stdout` 不含日志，断线不自动重放非幂等请求；
- [ ] stdio、HTTP 与 HTTPS 请求可通过同一 Request ID 完成审计追踪；
- [ ] Linux CI 全部通过；
- [ ] 不存在依赖开发者本机路径的测试。

---

## 22. 待决策事项

1. 首发平台是否仅支持 Linux，还是同时支持 macOS/Windows？
2. Worker 隔离采用本地进程、容器还是轻量 VM？
3. Policy Engine 使用内嵌规则、OPA/Rego 还是自研声明式策略？
4. Approval Service 由独立桌面 UI、IDE 扩展还是 MCP 宿主提供？
5. Gateway 与 Worker 采用 Unix socket、Named Pipe 还是本地 mTLS；远程 HTTP(S) API 采用请求/响应、SSE 还是双向流式协议？
6. 能力令牌采用非对称签名还是共享密钥 MAC？
7. 是否需要多租户和远程部署？
8. 审计日志保留周期及不可篡改存储方案是什么？
9. 是否允许项目仓库提供策略文件；若允许，哪些字段只能收紧？
10. 高风险插件是否与核心项目使用独立仓库和发布周期？

---

## 23. 推荐项目结构

```text
cmd/
  gateway/
  approval-ui/
  worker-read/
  worker-write/
  worker-analysis/
  worker-exec/
  worker-network/
internal/
  mcp/
  policy/
  capability/
  audit/
  identity/
  workspace/
  protocol/
pkg/
  encoding/
  smartedit/
  diff/
  codegraph/
  binanalysis/
platform/
  linux/
  darwin/
  windows/
policies/
  default/
  schema/
test/
  fixtures/
  integration/
  security/
  fuzz/
```

核心规则：`pkg/` 中的纯算法不得直接调用宿主文件系统或网络；所有外部访问必须经过 `internal/workspace` 或对应 Worker 的受控接口。

---

## 24. 最终建议

Secure Agent Tool 应作为一个新的安全核心项目建设，而不是在现有单体 MCP Server 上持续增加黑名单和前置校验。

现有项目应承担两个角色：

1. 功能和用户体验参考；
2. 编码、智能编辑、静态分析等纯算法模块的迁移来源。

新的安全边界必须建立在独立 Worker、操作系统沙箱、不可降级策略、短期能力令牌、带外审批和完整审计之上。只有这样，系统才能在保留现有工具优势的同时，将 AI 误操作、提示词注入和组件漏洞的影响控制在可接受范围内。
